```sh
LANG="zh_CN.utf8"
LANGUAGE="zh_CN.utf8:zh_CN.GB2312:zh_CN"
SUPPORTED="zh_CN.utf8:zh_CN:zh:en_US.UTF-8:en_US:en"
SYSFONT="lat0-sun16"

start_date=20180303 #${1}
end_date=20180831  #${2}

current_date=${start_date}


while [ ${current_date} != ${end_date} ]; do 

date=${current_date}
format_date=`date -d "-0 day ${current_date}" +%Y-%m-%d`

sh /home/zhaoningbo/ops/ops_brain_views_v2.sh ${current_date}

current_date=$(date -d "$current_date + 1 day" +%Y%m%d)
done
```